<tr align="center">
    <!-- эту строчку я вообще правильно сделал? -->
  <td><%= Category.where(id: test.category_id)[0].title %></td>
  <td><%= test.id %></td>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to 'Show', admin_test_path(test) %> </td>
  <td><%= link_to 'Edit', edit_admin_test_path(test) %> </td>
  <td>
    <%= link_to 'Delete',
                admin_test_path(test),
                method: :delete,
                data: { confirm: 'Are you sure?'} %>
  </td>
  <td><%= button_to 'Start', start_test_path(test) %></td> 
</tr>