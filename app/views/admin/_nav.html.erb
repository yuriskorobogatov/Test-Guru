<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('.admin_header')%> </span>

  <div class="nav-admin user">
    <b class="nav-admin user-email"><%=t('.nav_welcome', email: current_user.email) %></b>|
    <%= link_to "#{t('.logout')}", destroy_user_session_path, method: :delete%>
  </div>
</div>