<nav class="navbar navbar-light bg-light">
    <%= link_to 'TestGuru', root_path, class: 'navbar-brand'%>

    <span class="navbar-text">
    <% if user_signed_in? %>
      <p class="nav user"> <%= t('.welcome') %><b> &nbsp <%= current_user.email%> </b> &nbsp <%= t('.guru') %> </p>
      <p class="nav user"> <%= link_to "#{t('.log_out')}", destroy_user_session_path, method: :delete%></p>
    <% else%>  
      <p class="nav user"> <%= link_to "#{t('.log_in')}", new_user_session_path%>  </p>
      <p class="nav user"> <%= link_to "#{t('.registration')}", new_user_registration_path%> </p>
    <% end %>
    </span>
</nav>
